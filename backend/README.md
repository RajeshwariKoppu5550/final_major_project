# Backend Server

This is a simple Node.js/Express backend for your project.

## Setup

1. Install dependencies:
   ```bash
   npm install
   ```

2. Start the server:
   ```bash
   node server.js
   ```

The server will run on port 5000 by default.

## Example Route

- `GET /api/hello` — returns a JSON greeting. 